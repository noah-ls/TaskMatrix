# Support

Use GitHub Issues for bugs, feature requests, and documentation problems.

Before opening an issue:

- Search existing issues.
- Confirm you are using the latest available build or source from `main`.
- Include macOS version, Xcode version if building locally, and clear steps to
  reproduce the problem.

For security vulnerabilities, follow `SECURITY.md` instead of opening a public
issue with details.

